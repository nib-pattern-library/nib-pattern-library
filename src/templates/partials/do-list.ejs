<div class="guide-list guide-list--do">
    <ul class="icon-list icon-list--tick icon-list--green icon-list--aligned">
    <% items.forEach(function(item) { %>
    <li class="icon-list__item">
        <% if (item instanceof Array) { %>
            <%- partial('list', {items: item}) %>
        <% } else { %>
            <%- item %>
        <% } %>
    </li>
    <% }) %>
    </ul>
</div>